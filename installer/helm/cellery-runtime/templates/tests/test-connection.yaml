apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cellery-runtime.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "cellery-runtime.name" . }}
    helm.sh/chart: {{ include "cellery-runtime.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "cellery-runtime.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
